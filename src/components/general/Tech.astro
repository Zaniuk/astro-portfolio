---
import { EColors } from "@types";

const name = Astro.props.name as string; 
const color = Astro.props.color as EColors;
---

<div class={`w-fit flex flex-col items-center ${color || 'text-white'}` }>
  <slot/>
  <h3 class="text-lg">{name}</h3>
</div>